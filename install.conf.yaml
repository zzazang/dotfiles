- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.tmux.conf: tmux.conf
    ~/bin:
        path: bin/
        create: true
    ~/.zgen: zgen
    ~/.zshrc: zgen-zshrc
    ~/.ctags: ctags/ctags
    ~/.vim: vimdot
    ~/.vimrc: vimdot/vimrc

- shell:
    - ["git submodule update --init --recursive", "Installing submodules"]
    - ["echo ''", "Run 'cd vimdot && ./install.sh to install vim plugins."]

